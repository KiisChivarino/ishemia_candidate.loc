<form
        class="btn btn--delete"
        method="post"
        action="{{ path(
            (deleteRouteName is defined ? deleteRouteName : template.routes.delete),
        routParam is defined ? routParam : {'id': entity.id}
        ) }}"
        onsubmit="return confirm('{{ template.items.delete.content.deleteConfirm }}');"
>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entity.id) }}">
    <button>{{ template.items.delete.content.deleteButton }}</button>
</form>