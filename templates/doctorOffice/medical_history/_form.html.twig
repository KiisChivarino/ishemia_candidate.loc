{% import "recursive_form_macro.html.twig" as recursive_form_macro %}
{{ form_start(form) }}
<ul>
    {{ recursive_form_macro.recursiveForm(form) }}
</ul>
<div class="buttons">
    <button class="button main-button">{{ button_label|default(template.items.form.content.formButtonLabel) }}</button>
</div>
{{ form_end(form) }}